---
title: "Bar Chart"
description: Bar charts are commonly used to compare categorical data by representing each category with bars of varying lengths.
order: 3
published: true
references: ["https://recharts.org/en-US/api/BarChart"]
---

## Basic
Charts are used to display data in a clear and concise manner. They are ideal for presenting information in a visually appealing and easy-to-understand format.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-basic-demo" />

## Horizontal
Use horizontal to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-horizontal-demo" />


## Label
Use label to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-label-demo" />

## Inset Label
Use horizontal to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-horizontal-label-inset-demo" />

## Custom Label
Use custom label to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-custom-label-demo" />

## Mixed
Use mixed to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-mixed-demo" />

## Negative
Use negative to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-negative-demo" />

## Multiple
Use multiple to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-multiple-demo" />

## Stacked Legend
Use stackedLegend to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-stacked-legend-demo" />

## Active
Use active to display multiple data sets on the same chart.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-active-demo" />

## Controlled
Sometimes you might want to control the chart data dynamically.
<How withNoPadding={true} isCenter={false} toUse="charts/bar/bar-chart-controlled-demo" />

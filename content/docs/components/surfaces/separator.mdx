---
title: "Separator"
description: Lays down the access creds for a separator. It’s the visual boss that splits up chunks of content, like menu items or page sections.
status: new
order: 3
published: true
---

## Basic
Sometimes you need a separator to separate content. This component is a simple wrapper around the `Separator` component from `react-aria-components`. It's a breeze to use.

<How isCenter={false} toUse="surfaces/separator-demo" />

## Installation
<InstallCommand isAdd items={['separator']}/>

## Manual Installation
<InstallCommand isInstall isManual items={['react-aria-components']}/>
<SourceCode toShow={['separator']}/>

## Orientation
Separator has two orientations: `horizontal` and `vertical`. The default is `horizontal`.

<How toUse="surfaces/separator-orientation-demo" />

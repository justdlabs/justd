---
title: "Combo Box"
description: Drop down a list, type to narrow it down, like flipping through a phone book with a quick finder tool.
order: 1
published: true
references: ["https://react-spectrum.adobe.com/react-aria/ComboBox.html#props"]
---

## Combo Box

A combo box mixes a text input with a listbox, letting users filter options to match what they're lookin' for.
<How minW72 toUse="pickers/combo-box-demo" />

## Installation
<InstallCommand isAdd items={['combo-box']}/>


## Composed Components
<Composed components={['field', 'dropdown', 'popover', 'list-box', 'button']}/>

## Manual Installation

<InstallCommand isInstall isManual items={['react-aria-components', '@irsyadadl/paranoid']}/>

<SourceCode toShow={['combo-box']}/>
## Using Avatars

The combo box can be configured to use avatars instead of just regular text.
<How minW72 toUse="pickers/combo-box-avatar-demo" />

## Controlled
You can control the combo box by passing the `selectedKey` prop.
<How minW72 toUse="pickers/combo-box-controlled-demo" />

## Section Combo Box
A combo box shows a bunch of items that are grouped together.
<How minW72 toUse="pickers/section-combo-box-demo" />

## Validation
Just hit submit without picking an item, and boom! You'll peep that error message. What's the next move?
<How minW72 toUse="pickers/combo-box-validation-demo" />

## Readonly
When combo box is in a readonly state, it cannot be edited.
<How minW72 toUse="pickers/combo-box-readonly-demo" />

## Disabled
When combo box is disabled, it cannot be edited.
<How minW72 toUse="pickers/combo-box-disabled-demo" />
